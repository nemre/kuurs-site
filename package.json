{
  "devDependencies": {
    "@rollup/plugin-run": "^2.0.2",
    "prettier": "^2.3.0",
    "rollup": "^2.48.0"
  },
  "scripts": {
    "build": "rollup -c",
    "dev": "rollup -c -w",
    "format": "prettier -w .",
    "start": "node dist"
  }
}
